import { AppShell } from "@/components/AppShell";
import { ruleRows } from "@/lib/product/demo-account";

export default function RulesPage() {
  return <AppShell active="Rules"><main className="dashboard-shell rules-page"><div className="demo-banner" role="note"><span className="demo-dot" /> Illustrative rule version — not approved for a real prop-firm account.</div><div className="dashboard-topbar"><div><p className="eyebrow">Versioned rules engine</p><h1>Rule monitor</h1></div><span className="status-pill caution">Verification required</span></div><section className="rule-version-card"><div><small>ACTIVE PREVIEW VERSION</small><strong>Evaluation sandbox · v0.1-draft</strong><p>Effective only in the illustrative workspace</p></div><dl><div><dt>Verification</dt><dd>Draft</dd></div><div><dt>Platform</dt><dd>MT5</dd></div><div><dt>Calculation timezone</dt><dd>UTC (illustrative)</dd></div><div><dt>Official sources</dt><dd>None attached</dd></div></dl></section><section className="rule-detail-list">{ruleRows.map((rule, index) => <article className="rule-detail" key={rule.name}><div className="rule-detail-title"><span>{String(index + 1).padStart(2, "0")}</span><div><p className="eyebrow">Risk rule</p><h2>{rule.name}</h2></div><em className={rule.tone}>{rule.status}</em></div><div className="rule-detail-values"><span><small>CURRENT</small><strong>{rule.current}</strong></span><span><small>ALLOWED</small><strong>{rule.limit}</strong></span><span><small>BUFFER</small><strong>{rule.buffer}</strong></span></div><div className="calculation-note"><strong>Calculation</strong><p>{index === 0 ? "Start-of-day balance less a fixed allowance based on initial balance; evaluated against current equity." : index === 1 ? "Initial balance less the maximum-loss allowance; compared with the stricter active trailing floor." : index === 2 ? "Highest closed balance less the configured trail; capped so the floor does not rise beyond initial balance." : "Best qualifying day divided by total qualifying profit. No extra trading is recommended solely to alter this measure."}</p></div><div className="rule-source"><span>Source status</span><strong>No official source — illustrative only</strong><button type="button" disabled>Open source</button></div></article>)}</section><footer className="product-footer"><span>Rules cannot become active until validation, source attachment, review, and approval are complete.</span></footer></main></AppShell>;
}
